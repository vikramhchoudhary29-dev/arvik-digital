import AdminLayout from "@/components/admin/AdminLayout";import CreateCommissionButton from "@/components/admin/CreateCommissionButton";import DeleteEnquiryButton from "@/components/admin/DeleteEnquiryButton";import { prisma } from "@/lib/prisma";
export default async function AdminEnquiriesPage(){const enquiries=await prisma.enquiry.findMany({include:{partner:true,commission:true},orderBy:{createdAt:'desc'}});return <AdminLayout title="Enquiries" description="View website enquiries and partner referrals."><div className="grid gap-6">{enquiries.map(item=><div key={item.id} className="rounded-[28px] border border-white/10 bg-white/[.04] p-6"><div className="flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div><div className="mb-3 flex flex-wrap gap-3"><span className="rounded-full bg-yellow-400/10 px-3 py-1 text-xs font-bold text-yellow-200">{item.status}</span>{item.partner&&<span className="rounded-full bg-blue-400/10 px-3 py-1 text-xs font-bold text-blue-200">Partner: {item.partner.name} ({item.partner.referralCode})</span>}<span className="text-sm text-zinc-500">{new Date(item.createdAt).toLocaleString()}</span></div><h3 className="text-xl font-black">{item.name}</h3><p className="mt-1 text-sm text-zinc-400">{item.company||'No company'} • {item.service||'No service selected'}</p><p className="mt-3 text-sm text-zinc-400">Email: {item.email}</p><p className="mt-1 text-sm text-zinc-400">Phone: {item.phone||'Not provided'}</p><p className="mt-5 max-w-4xl leading-7 text-zinc-300">{item.message}</p>{item.commission&&<p className="mt-4 text-sm font-bold text-yellow-200">Commission: ₹{item.commission.amount.toLocaleString('en-IN')} ({item.commission.status})</p>}</div><div className="flex flex-wrap gap-3">{item.partner&&!item.commission&&<CreateCommissionButton enquiryId={item.id} rate={item.partner.commissionRate}/>}<DeleteEnquiryButton id={item.id}/></div></div></div>)}{enquiries.length===0&&<div className="rounded-[30px] border border-white/10 bg-white/[.04] p-10 text-center text-zinc-400">No enquiries found yet.</div>}</div></AdminLayout>}

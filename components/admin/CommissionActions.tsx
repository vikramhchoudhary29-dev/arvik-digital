"use client";
import { useState } from "react";import { useRouter } from "next/navigation";
export default function CommissionActions({id,status}:{id:string;status:string}){const r=useRouter();const [loading,setLoading]=useState(false);async function update(s:string){setLoading(true);await fetch(`/api/admin/commissions/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:s})});setLoading(false);r.refresh()}return <select disabled={loading} value={status} onChange={e=>update(e.target.value)} className="rounded-xl border border-white/10 bg-black px-3 py-2 text-sm"><option value="PENDING">Pending</option><option value="APPROVED">Approved</option><option value="PAID">Paid</option><option value="CANCELLED">Cancelled</option></select>}

"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function PartnerActions({id,status,rate}:{id:string;status:string;rate:number}){const r=useRouter();const [loading,setLoading]=useState(false);async function update(data:object){setLoading(true);await fetch(`/api/admin/partners/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});setLoading(false);r.refresh()}return <div className="flex flex-wrap gap-2"><select disabled={loading} value={status} onChange={e=>update({status:e.target.value})} className="rounded-xl border border-white/10 bg-black px-3 py-2 text-sm"><option value="PENDING">Pending</option><option value="APPROVED">Approved</option><option value="REJECTED">Rejected</option><option value="INACTIVE">Inactive</option></select><button disabled={loading} onClick={()=>{const v=prompt('Commission percentage',String(rate));if(v!==null&&!Number.isNaN(Number(v)))update({commissionRate:Number(v)})}} className="rounded-xl border border-yellow-400/20 px-3 py-2 text-sm font-bold text-yellow-200">{rate}% Rate</button></div>}
